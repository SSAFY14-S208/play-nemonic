// 익명 사용자 발급 시 백엔드가 부여하는 기본 닉네임. 사용자가 patchAnonymousNickname
// 으로 갈아치우기 전까지는 이 값이 그대로 store에 남아 있다.
//
// 닉네임이 이 기본값이면 일부 도메인 액션(릴레이 방 생성/입장 등)이 백엔드에서
// 거부되므로, 클라이언트가 미리 막아 닉네임 변경 모달로 유도하는 데 쓴다.
export const DEFAULT_USER_NICKNAME = '익명'
