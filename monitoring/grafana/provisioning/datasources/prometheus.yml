# Grafana datasource auto-provisioning
# 기동 시 Prometheus를 자동으로 등록 — UI에서 수동 추가 불필요

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
